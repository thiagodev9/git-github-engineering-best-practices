# git init

O comando `git init` cria um novo repositório Git no diretório atual.

Uso básico:

```bash
git init
```

Quando usar:

- para começar a versionar um projeto existente
- ao criar um novo projeto do zero

Após executar, o Git passa a monitorar as mudanças no diretório.
